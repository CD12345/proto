// Percussion Groove - Polyrhythmic drums
stack(
  sound("bd*4")
    .gain(0.9),
  sound("~ sd ~ sd")
    .gain(0.7),
  sound("hh*8")
    .gain(0.4),
  sound("~ ~ cp ~")
    .gain(0.6),
  sound("[~ ~ ~]*3 tabla:2")
    .gain(0.5)
    .speed(rand.range(0.9, 1.1)),
  sound("~ [~ shaker]*2 ~")
    .gain(0.35)
).cpm(110)
