<Window x:Class="CppArrayExporter.UI.ExportSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Export Array Settings"
        Height="500"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,10,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Variable Name -->
        <StackPanel Grid.Row="0">
            <Label Content="Variable Name:"/>
            <TextBox x:Name="VariableNameTextBox" IsReadOnly="True" Background="#F0F0F0"/>
        </StackPanel>

        <!-- Length Expression -->
        <StackPanel Grid.Row="1">
            <Label Content="Length Expression:"/>
            <TextBox x:Name="LengthExpressionTextBox"
                     ToolTip="Enter a number or expression (e.g., 'arraySize', '{count * 2}')"/>
            <TextBlock Margin="0,0,0,5"
                       FontSize="11"
                       Foreground="Gray"
                       TextWrapping="Wrap">
                Enter a number or expression that evaluates at runtime. Use curly braces for complex expressions (e.g., {count * 2}).
            </TextBlock>
        </StackPanel>

        <!-- Export Action -->
        <StackPanel Grid.Row="2">
            <Label Content="Export Action:"/>
            <ComboBox x:Name="ExportActionComboBox"
                      DisplayMemberPath="DisplayName"
                      SelectionChanged="ExportActionComboBox_SelectionChanged"/>
            <TextBlock x:Name="ExportActionDescription"
                       Margin="0,5,0,5"
                       FontSize="11"
                       Foreground="Gray"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Export Action Settings -->
        <StackPanel Grid.Row="3">
            <Label Content="Export Action Settings:"/>
        </StackPanel>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <StackPanel x:Name="SettingsPanel"/>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel"
                    Width="80"
                    Height="30"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
